apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pvc-immich-postgres
  namespace: photos
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-path
  resources:
    requests:
      storage: 10Gi
