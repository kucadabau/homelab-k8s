apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pvc-immich-ml
  namespace: photos
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-path
  resources:
    requests:
      storage: 10Gi
